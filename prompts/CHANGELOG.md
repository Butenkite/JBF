# Prompt changelog

All dates use ISO-8601 (`YYYY-MM-DD`). Prompt changes are versioned independently of application semver.

## 2026-04-28 — `tailor-v1.md` initial

- Introduced strict JSON-only output aligned with `packages/contracts/schemas/tailor-output-v1.json`.
- Documented placeholder tokens for future Epic C wiring (`{{master_resume}}`, `{{jd_text}}`, etc.).
- Emphasized no fabrication of facts; ids must come from master resume bullet comments.
